{
    "name": "shares-slon-plus",
    "version": "1.0.0",
    "description": "TRC20 token contract for Shares SlonPlus",
    "main": "scripts/deploy.js",
    "scripts": {
        "test": "truffle test",
        "migrate": "truffle migrate",
        "deploy": "node scripts/deploy.js"
    },
    "dependencies": {
        "@openzeppelin/test-helpers": "^0.5.16",
        "ethers": "^5.0.0",
        "truffle": "^5.11.5",
        "web3": "^1.0.0"
    },
    "devDependencies": {
        "chai": "^4.2.0",
        "mocha": "^6.0.0"
    }
}
